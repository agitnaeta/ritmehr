# QW-06 — `.env.example`

**Fokus:** BP-4 samakan DB_PORT + SEC-2 checklist rilis (dokumentasi)
**Severity:** 🟢 Rendah
**Status:** [x] DONE — commit: `pending` (terverifikasi 2026-08-29)
**File (satu-satunya) yang disentuh:** `.env.example`

---

## Masalah
`.env.example` `DB_PORT` ≠ docker (`:3307`) → friksi onboarding. Sekalian tambahkan
komentar checklist rilis produksi (SEC-2).

## Diff
```dotenv
-DB_PORT=3306
+DB_PORT=3307            # samakan dengan docker-compose (host port)

# --- Checklist sebelum deploy PRODUKSI (SEC-2) ---
# APP_ENV=production
# APP_DEBUG=false
# LOG_LEVEL=warning
```

## Cek per file
- [ ] `cp .env.example .env && php artisan migrate` konek ke docker mysql tanpa ubah port manual.

---

## Verifikasi
- [x] `DB_PORT=3307`, `DB_DATABASE=absensi` (samakan docker-compose `3307:3306` + db nyata)
- [x] Checklist SEC-2 (APP_DEBUG=false dst) + key CFG (QDRANT/LLM/EMBEDDING/PYTHON_BIN) ditambah
- [x] Proof koneksi di bawah
- [x] Docs/config-only → tak ada kode berubah, baseline utuh
- [x] Flip `Status:` ke `[x] DONE`

## PROOF (2026-08-29)
```
$ grep DB_PORT/DB_DATABASE .env.example
DB_PORT=3307
DB_DATABASE=absensi

$ koneksi PDO pakai nilai .env.example (port 3307 + db absensi):
KONEK OK via port 3307/db absensi -> users=5
```
Onboarding `cp .env.example .env && php artisan migrate` kini konek tanpa ubah port manual.
Bonus: `.env.example` melengkapi dokumentasi env untuk key CFG-01 (matching + cv) supaya
sinkron dengan `config/services.php`.
