# QW-06 — `.env.example`

**Fokus:** BP-4 samakan DB_PORT + SEC-2 checklist rilis (dokumentasi)
**Severity:** 🟢 Rendah
**Status:** [ ] TODO — commit: `______`
**File (satu-satunya) yang disentuh:** `.env.example`

---

## Masalah
`.env.example` `DB_PORT` ≠ docker (`:3307`) → friksi onboarding. Sekalian tambahkan
komentar checklist rilis produksi (SEC-2).

## Diff
```dotenv
-DB_PORT=3306
+DB_PORT=3307            # samakan dengan docker-compose (host port)

# --- Checklist sebelum deploy PRODUKSI (SEC-2) ---
# APP_ENV=production
# APP_DEBUG=false
# LOG_LEVEL=warning
```

## Cek per file
- [ ] `cp .env.example .env && php artisan migrate` konek ke docker mysql tanpa ubah port manual.

---

## Verifikasi
- [ ] `php -l .env.example` bersih (kalau file PHP)
- [ ] `php -d memory_limit=2G -d xdebug.mode=off vendor/bin/phpunit --no-coverage` → tetap hijau (baseline)
- [ ] `node tests/browser/crud-suite.mjs` → tetap hijau (baseline 146)
- [ ] Verifikasi manual di browser sesuai bagian "Cek" di atas
- [ ] Flip `Status:` ke `[x] DONE` + isi commit SHA setelah semua centang
